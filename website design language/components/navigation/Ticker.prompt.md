Slow uppercase marquee between hairlines — the site's one piece of ambient motion. One per page, always at the very top, full-bleed.

```jsx
<Ticker />
<Ticker text="CASE STUDY — Q-LEARNER — REINFORCEMENT LEARNING" duration={60} />
<Ticker tone="blue" />
```

Slower is calmer; never faster than the 46s default. `tone="blue"` only for special chrome moments.
